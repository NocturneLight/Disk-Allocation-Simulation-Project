To compile  the program:
   	    You type in "make".

To delete object files  and othe things:
   	    You type in "make clean".

To run  the program:
   	    You type in "ProjectThree" and then type in either "contiguous", "indexed", or "chained".
